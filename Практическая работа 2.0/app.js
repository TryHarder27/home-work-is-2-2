function calculateDifference(a, b) {
    return (a - b);
}

const num1 = 54;
const num2 = 46;
const difference = calculateDifference (num1, num2);
console.log(`Разница между первым и вторым числом: ${num1} - ${num2} ответ: ${difference}.`);